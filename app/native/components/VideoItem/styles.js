import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  itemContainer: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    padding: 16,
  },
  coverPhoto: {
    width: 80,
    height: 60,
  },
  title: {
    marginLeft: 16,
  },
});

export default styles;
